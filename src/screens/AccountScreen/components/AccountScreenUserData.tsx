import React from 'react';
import { View, Text } from 'react-native';

const AccountScreenUserData = () => {
    return (
        <View>
            <Text>UserData...</Text>
        </View>
    );
};
export default AccountScreenUserData;
