import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        textAlign: 'center',
        fontSize: 28,
        fontWeight: 'bold',
        marginTop: 50,
        marginBottom: 15,
    },
    input: {
        height: 50,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderRadius: 5,
    },
    container: {
        flex: 1,
    },
    fild: {
        margin: '0 10',
    },
});
export default styles;
