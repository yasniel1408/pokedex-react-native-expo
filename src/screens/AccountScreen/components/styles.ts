import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        textAlign: 'center',
        fontSize: 28,
        fontWeight: 'bold',
        marginTop: 50,
        marginBottom: 15,
    },
    input: {
        height: 45,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderRadius: 5,
    },
    containerBtn: {
        marginRight: 12,
        marginLeft: 12,
        borderRadius: 5,
    },
});
export default styles;
