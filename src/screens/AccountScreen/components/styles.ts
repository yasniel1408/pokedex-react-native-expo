import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginRight: 12,
        marginLeft: 12,
    },
    title: {
        textAlign: 'center',
        fontSize: 28,
        fontWeight: 'bold',
        marginTop: 50,
        marginBottom: 15,
    },
    input: {
        height: 50,
        marginTop: 16,
        marginBottom: 7,
        borderWidth: 1,
        padding: 10,
        borderRadius: 5,
    },
    text: {
        height: 50,
        marginTop: 10,
        marginBottom: 10,
        borderWidth: 1,
        padding: 10,
        borderRadius: 5,
        fontSize: 15,
    },
    containerBtn: {
        marginTop: 12,
        borderRadius: 5,
    },
});
export default styles;
