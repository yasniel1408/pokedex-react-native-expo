import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    content: {
        marginHorizontal: 20,
        marginTop: 50,
    },

    loading: {
        marginTop: 300,
    },
});

export default styles;
